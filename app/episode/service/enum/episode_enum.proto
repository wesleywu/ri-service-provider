syntax = "proto3";

package proto;

option go_package = "github.com/wesleywu/ri-service-provider/app/episode/service/enum;enum";

enum ContentType {
  NEWS           = 0;  // 新闻
  COMEDY         = 1;  // 喜剧
  SPORTS         = 2;  // 体育
}

enum FilterType {
  RULED          = 0; // 规则筛选
  MANUAL         = 1; // 人工
}

